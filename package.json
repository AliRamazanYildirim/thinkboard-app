{
  "name": "thinkboard-app",
  "private": true,
  "version": "1.0.0",
  "description": "Fullstack monorepo: Thinkboard App (Bun, React, Express, MongoDB, Redis)","scripts": {
  "build": "cd frontend && bun install && bun run build",
  "start": "cd backend && bun install && bun run start"
},
  "engines": {
    "bun": ">=1.0.0"
  },
  "keywords": [
    "monorepo",
    "bun",
    "react",
    "express",
    "mongodb",
    "redis"
  ],
  "author": "",
  "license": "MIT"
}
